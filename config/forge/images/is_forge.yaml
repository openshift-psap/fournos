apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  name: forge-core
  labels:
    app: forge
    component: image-build
  annotations:
    description: "FORGE core image for remote job execution"
spec:
  lookupPolicy:
    local: true
  tags:
    - name: main
      annotations:
        description: "Main branch build of FORGE core"
        tags: "latest,main"
