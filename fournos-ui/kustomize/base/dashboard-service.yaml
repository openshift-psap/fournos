apiVersion: v1
kind: Service
metadata:
  name: fournos-dashboard
  namespace: fournos-dashboard
  labels:
    app.kubernetes.io/name: fournos-dashboard
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      protocol: TCP
      targetPort: 8000
  selector:
    app.kubernetes.io/name: fournos-dashboard
